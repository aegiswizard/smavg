# Smavg Agent Plugin

This folder is a public example of the Smavg plugin bundle.

The plugin is not a separate Smavg engine.

It tells agent hosts how to call the local Smavg core.

Use:

```bash
smavg plugin build
smavg plugin verify
```
