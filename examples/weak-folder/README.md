# Weak Folder Example

This folder is intentionally not very repetitive.

Smavg should report weak or limited benefit when structure is not strong.

That is a good result because it proves Smavg is not pretending.

Try:

```bash
smavg context examples/weak-folder --out /tmp/weak.md --json /tmp/weak.json
```
